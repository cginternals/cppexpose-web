extends partials/index.pug


block vars
  - var navActive = 0 

block head
  // link(rel = 'stylesheet' href = 'css/template.css')
  // script(src = 'js/template.js')

block header
  section.container
    header.jumbatron
      img(src = 'img/cppexpose-logo.svg' alt = 'cppexpose logo').mb-2.w-50.d-none.d-md-block
      img(src = 'img/cppexpose-logo.svg' alt = 'cppexpose logo').mb-2.d-block.d-md-none
      p.lead cppexpose is a cross-platform C++11 library that provides tools for introspection of types, properties, and classes.

block content
  section.container
    div.row
      div.col
        include:markdown-it(html plugins=['markdown-it-highlightjs']) README.md

  section.container
    div.row
      div.col
        include:markdown-it(html plugins=['markdown-it-highlightjs']) SUPPORT.md
